# 服务器配置
PORT=3000

# ========== 自动扩词配置（服务器端使用）==========

# 单个 Key（用于 AI 分类识别等）
OPENAI_API_KEY=sk-your-key

# Key 池（逗号分隔，用于自动批量扩词，轮流使用）
# 示例: OPENAI_KEY_POOL=sk-key1,sk-key2,sk-key3
OPENAI_KEY_POOL=

# 模型选择
OPENAI_MODEL=gpt-4o-mini

# 可选：自定义 API 地址（如使用代理）
# OPENAI_BASE_URL=https://api.openai.com/v1
