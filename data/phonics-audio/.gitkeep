# 真人发音音频目录

将发音音频文件放入此目录。

## 命名规则

| 发音模式 | 文件名示例 |
|---------|-----------|
| 字母 a | `a.mp3` 或 `A.mp3` |
| 组合 sh | `sh.mp3` |
| Magic E | `a_e.mp3` |

## 支持格式

- MP3 (推荐)
- WAV
- OGG

## 获取音频

你可以：
1. 自己录制发音
2. 从专业发音库获取
3. 使用 TTS 生成后手动优化
